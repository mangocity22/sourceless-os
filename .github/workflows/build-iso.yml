name: Build ISO

on:
  workflow_dispatch:

jobs:
  build-iso:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Log in to GHCR with sudo
        run: |
          sudo podman login ghcr.io -u ${{ github.actor }} -p ${{ secrets.GITHUB_TOKEN }}

      - name: Pull Target OS Image
        run: |
          sudo podman pull ghcr.io/${{ github.repository_owner }}/sourceless-os:latest

      - name: Build ISO with Bootc Image Builder
        run: |
          mkdir -p output
          sudo podman run --rm --privileged \
            -v /var/lib/containers/storage:/var/lib/containers/storage \
            -v ./output:/output \
            quay.io/centos-bootc/bootc-image-builder:latest \
            --type iso \
            --rootfs ext4 \
            ghcr.io/${{ github.repository_owner }}/sourceless-os:latest

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        with:
          name: sourceless-os-iso
          path: output/
          retention-days: 7